server:
  port: 8443
spring:
  application:
    name: gateway-web
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:10.0.0.110}:${REGISTER_PORT:8848}
        namespace: ${REGISTER_NAMESPACE:aba811bc-87ee-4179-92a5-398b5684d55c}
        group: k12-${CONFIG_SUFFIX:dev}
        ip: ${SELF_HOST:10.19.0.16}
      config:
        server-addr: ${REGISTER_HOST:10.0.0.110}:${REGISTER_PORT:8848}
        file-extension: yml
        namespace: ${CONFIG_NAMESPACE:352312ed-57d2-4e7c-b160-fe2b2f0dce6f}
        group: java
        ext-config:
          - data-id:
              config-${CONFIG_SUFFIX:dev}.yml
            group:
              common
            refresh:
              true
          - data-id: # node服务的白名单
              nodejs-whitelist-${CONFIG_SUFFIX:dev}.yml
            group:
              nodejs
            refresh:
              true
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD_HOST:10.0.0.110}:${SENTINEL_DASHBOARD_PORT:18021}